export interface StaticPost {
  id: string
  title: string
  slug: string
  excerpt: string
  content: string
  created_at: string
  cover_image: string | null
  published: boolean
}

export const staticPosts: StaticPost[] = [
  {
    id: 'static-blog-dev-deploy-skill',
    title: '把 AI 开发博客到部署验证沉淀成一个 Skill',
    slug: 'blog-dev-deploy-skill',
    excerpt:
      '这篇文章记录我是如何把“用 AI 开发、修复、部署并验证一个博客系统”的完整流程，提炼成一个可复用的 Codex Skill。',
    created_at: '2026-04-24T18:00:00.000Z',
    cover_image: null,
    published: true,
    content: `# 把 AI 开发博客到部署验证沉淀成一个 Skill

这次我没有只停留在“把博客修好并上线”。

我进一步把整个过程提炼成了一个可以复用的 **Codex Skill**，名字叫 \`blog-dev-deploy\`。这样以后再做同类项目时，AI 不需要从零摸索，而是可以直接按一套已经验证过的流程推进。

## 为什么要做这个 Skill

在实际协作里，我发现“开发一个博客”并不只是写页面：

- 要先弄清当前技术栈和部署方式
- 要判断这是静态站还是动态博客
- 要修复代码、文案、编码和路由问题
- 要处理数据库、鉴权、评论、点赞等能力
- 要在部署前完成本地构建验证
- 要在部署后做线上检查
- 最后还要把状态、决策和记忆沉淀下来

如果这些步骤每次都重新走一遍，成本很高；如果把它们整理成 Skill，后面就能稳定复用。

## 这个 Skill 解决什么问题

\`blog-dev-deploy\` 主要解决这几类任务：

1. **新建博客系统**
2. **修复已有博客项目**
3. **统一部署到生产环境**
4. **验证线上版本是否真的可用**
5. **把项目经验沉淀成长效记忆**

它特别适合下面这种技术路线：

- Next.js
- TypeScript
- Tailwind CSS
- Supabase
- Vercel

## Skill 里的核心流程

我把流程拆成了几个明确阶段。

### 1. 先读上下文

AI 不应该上来就改代码，而是要先看清这些内容：

- 仓库结构
- 环境变量
- 当前部署平台
- 历史日志和记忆文档
- 博客是静态方案还是动态方案

### 2. 先确认目标

要先分清：

- 哪些是长期稳定需求
- 哪些只是本轮临时需求
- 当前最大的阻塞点是什么

### 3. 按博客系统的真实顺序建设

Skill 里固定了一套比较稳的实现顺序：

1. 应用外壳
2. 首页、列表页、详情页、关于页
3. 后台与发布流程
4. 评论、点赞等互动能力
5. UI 文案、编码、元信息清理

### 4. 正确连接后端

这里我特别强调了安全规则：

- 匿名 key 只用于前端允许的场景
- service role key 只能留在服务端
- 不能把高权限密钥硬编码进源码

### 5. 先本地验证再部署

Skill 明确要求：

- 至少要跑一次生产构建
- 必须先修掉 build 和 type 问题
- 再进入线上部署

### 6. 部署后必须验证

不是“部署成功”就算结束，而是要继续验证：

- 首页是否正常
- 博客列表页是否正常
- 关于页是否正常
- 登录页是否正常
- 至少一篇文章详情页是否正常

## 这次博客项目里，Skill 是怎么来的

这次我实际做了这些事情：

- 修复关键页面中文乱码
- 修复标题到 slug 的生成问题
- 移除导入接口里的硬编码高权限密钥
- 跑通本地生产构建
- 重新部署到 Vercel
- 验证线上首页、博客页、关于页、登录页全部返回 200
- 把流程整理成 Skill 并完成校验

也就是说，这个 Skill 不是“空想出来的模板”，而是从一次真实交付里反向抽象出来的。

## 这个 Skill 的价值

我觉得它最有价值的地方有三个。

### 第一，减少重复探索

AI 下次再接手博客项目时，不需要重新判断一遍“该先看什么、先做什么、怎么验证”。

### 第二，保证交付闭环

它把完整链路固定了下来：

**需求 → 修复 → 验证 → 部署 → 线上检查 → 记忆沉淀**

### 第三，方便长期协作

当项目变多以后，真正重要的不是某一次回答，而是能不能形成一套能稳定复用的协作能力。

## 这个 Skill 现在已经可用了吗

这次创建的 Skill 已经通过校验，并安装到了本地技能目录。

它的作用不是替代思考，而是给 AI 一条已经跑通过的工作路径。

## 下一步还能怎么扩展

我接下来还想继续把它拆得更细，比如再做这些专题：

- 博客 SEO Skill
- 博客图片上传 Skill
- 博客内容运营 Skill
- 博客数据统计 Skill

这样后面不只是“能做博客”，而是可以把博客系统的长期演进也模块化。

## 总结

真正高效的 AI 协作，不是一次次临时发挥，而是不断把成功流程沉淀成可以复用的能力。

这次这篇文章对应的 \`blog-dev-deploy\` Skill，就是把“AI 开发博客并部署验证”这件事，从一次完成，变成了一项可以重复调用的能力。`,
  },
]

export function getStaticPostBySlug(slug: string) {
  return staticPosts.find((post) => post.slug === slug)
}
